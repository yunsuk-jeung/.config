**/.DS_Store
*.bak

# Raycast keeps its own extension cache here; only our scripts are worth tracking.
raycast/*
!raycast/scripts/

# Installed by tpm via bootstrap.sh, never committed.
tmux/plugins/
