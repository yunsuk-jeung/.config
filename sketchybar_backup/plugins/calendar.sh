#!/bin/bash

#sketchybar --set $NAME icon="$(date '+%a %d. %b')" label="$(date '+%H:%M')"
# sketchybar --set "$NAME" label="$(date '+%m/%d %H:%M')"
sketchybar --set $NAME icon=""  label="$(date '+%a %d. %b %H:%M')"

